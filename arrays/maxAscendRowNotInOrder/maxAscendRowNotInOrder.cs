using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace maxAscendRowNotInOrder
{
    class maxAscendRowNotInOrder
    {
        static void Main(string[] args)
        {
            /*
            int[] array = { 9, 6, 2, 7, 4, 7, 6, 5, 8, 4 };
            int[] temp = new int[array.Length];
            int[] bestLen = new int[array.Length];
            for (int i = 0; i < array.Length; i++)
            {
                for (int z = 0; z < array.Length-1; z++)
                {
                    if (array[i] < array[z])
                    {
                        temp[i] = array[i];

                    }
                }
            }
            foreach (int var in temp)
            {
                Console.WriteLine(var);
            }
            Console.WriteLine();
            */
        }
    }
}
